<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="darwingapp.com">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/mydrawnappiconandwelcomer"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mydrawnappiconandwelcomer"
        android:supportsRtl="true"
        android:theme="@style/Theme.DarwingApp">
        <activity
            android:name=".InformationOnApp"
            android:exported="false" />
        <activity
            android:name=".Search"
            android:exported="false" />
        <activity
            android:name=".viewart"
            android:exported="false" />
        <activity
            android:name=".Drawing"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>